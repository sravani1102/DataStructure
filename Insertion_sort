#include<stdio.h>
void insertion_sort(int [],int);
void main()
{
    int a[20],n,i;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    insertion_sort(a,n);
}
void insertion_sort(int a[],int n)
{
    int i,j,temp;
    for(i=1;i<n;i++)
    {
        j=i;
        temp=a[i];
        while(j>0 && temp<a[j-1])
        {
            a[j]=a[j-1];
            j--;
        }
        a[j]=temp;
    }
    for(i=0;i<n;i++)
        printf("%d ",a[i]);
}
